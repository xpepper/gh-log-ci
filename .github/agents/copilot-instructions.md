# gh-log-ci Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-01-06

## Active Technologies

- Bash (compatible with `/usr/bin/env bash`) + `gh` CLI (GitHub CLI), `git`, `jq` (via gh) (001-graphql-batch-query)

## Project Structure

```text
src/
tests/
```

## Commands

# Add commands for Bash (compatible with `/usr/bin/env bash`)

## Code Style

Bash (compatible with `/usr/bin/env bash`): Follow standard conventions

## Recent Changes

- 001-graphql-batch-query: Added Bash (compatible with `/usr/bin/env bash`) + `gh` CLI (GitHub CLI), `git`, `jq` (via gh)

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
